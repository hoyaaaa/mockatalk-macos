# Prototypes

Use this directory for throwaway experiments.

Expected first prototype:

1. Reuse macSandbox baseline.
2. Create persistent overlay.
3. Boot with QEMU.
4. Connect through external `sdl-freerdp`.
5. Install and launch Windows KakaoTalk.

Only after this works should we build the polished macOS app shell.

